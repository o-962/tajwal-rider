import 'package:flutter/material.dart';

class OptionBoxWidget extends StatelessWidget {
  const OptionBoxWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}